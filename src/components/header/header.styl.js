import styled from 'styled-components';

const HeaderHolder = styled.header `
  background: black;
  color: #eee;
  text-align: center;
  font-size: 24px;
  padding: 15px 0;
  margin-bottom: 10px;
`;

export default HeaderHolder;
